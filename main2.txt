let compute: fn(int, int) -> int = fn(x, y) {
        let result: int = if (x > y) {
            return x + y;
        } else {
            return x - y;
        };
        return result;
    };

let x: int = 20;
let y: int = 22;